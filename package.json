{
  "name": "junkan",
  "private": true,
  "version": "2.0.0",
  "description": "A web app that cycles through a list of websites each one after another in an infinite loop",
  "main": "",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "ghup": "npm run dist && git commit -a -m 'Updates the dist' && git checkout gh-pages && git checkout master -- dist && git commit -a -m 'Updates the dist' && git push origin gh-pages && git checkout master",
  },
  "repository": "https://github.com/C3-TKO/junkan",
  "keywords": [],
  "author": "Thomas Kolar",
  "dependencies": {
    "react": "^16.3.0",
    "react-dom": "^16.3.0",
    "react-scripts": "1.1.1"
  }
}
